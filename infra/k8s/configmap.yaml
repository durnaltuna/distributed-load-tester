apiVersion: v1
kind: ConfigMap
metadata:
  name: load-tester-config
  namespace: load-tester
data:
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  DB_HOST: timescaledb
  DB_PORT: "5432"
  DB_NAME: loadtest
  PORT: "3000"
  VITE_ORCHESTRATOR_HTTP: http://localhost:3000
  VITE_ORCHESTRATOR_WS: ws://localhost:3000